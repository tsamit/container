apiVersion: v1
kind: Pod
restart: always
metadata:
  name: nextcloud
  labels:
    name: nextcloud
spec:
  containers:   
    - name: nextcloud
      image: nextcloud
      args: ["app:nextcloud"]
      env:
        - name: TZ
          value: Asia/Bangkok
        - name: DEBUG
          value: "false"

        - name: NEXTCLOUD_URL
          value: http://cloud.jasmine.com

        - name: NEXTCLOUD_ADMIN_USER
          value: admin
        - name: NEXTCLOUD_ADMIN_PASSWORD
          value: password

        - name: NEXTCLOUD_UPLOAD_MAX_FILESIZE
          value: 4G
        - name: NEXTCLOUD_MAX_FILE_UPLOADS
          value: "20"

        - name: DB_TYPE
          value: pgsql
        - name: DB_HOST
          value: 10.2.0.130
        - name: DB_PORT
          value: "5432"
        - name: DB_USER
          value: nextcloud
        - name: DB_PASS
          value: 4awefa3
        - name: DB_NAME
          value: nextcloud

      ports:
        - name: php-fpm
          containerPort: 9000
          protocol: TCP
      volumeMounts:
        - mountPath: /var/lib/nextcloud
          name: data
      livenessProbe:
        tcpSocket:
          port: 9000
        initialDelaySeconds: 5
        timeoutSeconds: 1
  volumes:
    - name: data
      emptyDir: {}
